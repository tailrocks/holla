# Implementation Plans

Generated by the improve skill on 2026-07-16 at holla commit `ad8a0f1`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Operator directives baked into every plan:
- All TUI must be built from TermRock (https://github.com/tailrocks/termrock),
  pinned at the **latest** `main` revision at execution time (resolve via
  `git ls-remote`, then apply any `migrations/` files newer than
  `0002-v0.8.0`). API excerpts in the plans were verified at
  `da54a033f368ed0888af90ae43d19bcb96fb8581` (v0.8.0).
- GPL-licensed references (Mole GPL-3.0, ncdu, gdu, macdirstat): **ideas
  and taxonomy only — never copy code.** Linkable deps must be
  Apache-2.0/MIT/BSD/MPL-2.0.
- Commits: Conventional Commits with DCO sign-off (`git commit -s`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | PRODUCT.md — mission, scope, UX flows, roadmap | P1 | S | — | DONE |
| 002 | Test baseline + four confirmed bug fixes | P1 | M | — | TODO |
| 003 | TermRock migration of the launcher menu | P1 | L | 002 | TODO |
| 004 | Task runner: streaming output + cancellation | P1 | L | 003 | TODO |
| 005 | Provider/Action model, async probe, fuzzy search, confirmations | P1 | L | 003, 004 | TODO |
| 006 | Disk scan engine (streaming, cancellable, APFS-correct) | P2 | L | 002 | TODO |
| 007 | TermRock extensions (multi-select, metadata cells, progress, log pane) | P2 | L | 003 | TODO |
| 008 | Disk analyzer TUI + validated deletion choke point | P2 | L | 005, 006, 007 | TODO |
| 009 | macOS cleanup insights taxonomy | P3 | L | 005, 006, 008 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 003 needs 002's characterization tests to preserve menu content through
  the UI rewrite (and 002's probe field renames land first).
- 004 needs 003's Session/Theme/Keymap pattern.
- 005 needs 003 (list/search UI base) and 004 (actions execute through the
  streaming runner).
- 006 is UI-independent: can run in parallel with 003–005 (needs only 002's
  test conventions).
- 007 happens in the termrock repo; 008 hard-depends on its metadata cells,
  multi-select, and progress widget. 004 may optionally adopt its LogPane
  later.
- 008 introduces `src/cleanup/` — the single deletion choke point; 009
  builds on it and migrates the legacy gradle/idea deleters to it.
- Selection default if run non-interactively: plans were written for the
  full operator-requested scope (termrock migration, launcher, disk
  analyzer, cleanup taxonomy) — all 9 selected by the operator's brief.

## Findings considered and rejected / deferred

- **`tabled`, `indicatif`, `owo-colors` unused deps** — folded into plan
  003's cleanup step rather than a standalone plan.
- **TermRock `Theme` not constructible (phosphor-only)** — real limitation,
  deliberately deferred: holla ships with the tailrocks phosphor theme;
  revisit only if branding diverges (noted in plan 007).
- **`neo_frizbee` (fff's SIMD matcher) over nucleo** — rejected for now:
  holla matches hundreds of items, not millions; `nucleo-matcher` is the
  maintained, boring choice (plan 005 records the reasoning).
- **Embedding `pdu`/`dua` as the scan engine** — rejected: pdu is
  batch-only (no streaming), dua isn't a library; own engine per plan 006.
- **`jwalk`/`rayon` walker** — rejected in favor of a fixed worker pool +
  `DirReader` trait (custom stat handling, per-thread dataless policy);
  plan 006 records the friction-report escape hatch.
- **Finder/AppleScript trash integration (Mole's approach)** — rejected:
  requires GUI session; rename-into-`~/.Trash` is SSH-safe (plan 008).
- **sudo/system-domain cleanup, app-uninstall residue, Time Machine
  snapshot reporting, Spotlight `mdfind` large-file discovery, size
  caching between runs, frecency ranking for the launcher** — deferred
  with reasons in plans 008/009 maintenance notes; none blocks the core
  flows.
- **`has_mise_toml` dead-code field in Probe** — dies naturally in plan
  005's provider refactor; not worth a standalone fix.

## Source-audit findings ledger (verified at `ad8a0f1`)

Fixed by plan 002: parent-vs-current repo-set mismatch
(`src/probe.rs:116-125` vs `src/commands/git.rs:66-76`); dead omz feature
(`src/probe.rs:44`); shell interpolation in `push_all_remotes`
(`src/commands/git.rs:40-56`); unbounded `compose logs -f`
(`src/tui/menu.rs:122-127`).
Fixed by plan 004: buffered-until-exit output + sequential UI freeze + no
cancellation (`src/tui/app.rs:89-124`).
Fixed by plan 005: blocking startup probe (`src/main.rs:14`,
`src/probe.rs:37-63`); destructive actions without confirmation (menu Enter
path); anonymous closure actions (no ids/keywords/danger).
