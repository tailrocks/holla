# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/holla"
profile = "generic"
verified = true
default_branch = "main"
runners = "both"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-present", "renovate-configuration", "rust-binaries:holla-cli:1", "rust-build-script:holla-cli", "rust-package", "rust-test-targets:holla-cli:1"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files.", "Rust build scripts are watched but their system dependencies and generated outputs are not inferred."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = ["self-hosted", "velnor-target-mvp"]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml", "preview.yml", "release.yml"]
notes = ["Rust verification uses Mr. Boxington 1.11.1 by default on both lanes. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Velnor jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = true
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."
kind = "homebrew"
package = "holla"
packages = []
binary = "holla"
targets = ["x86_64-unknown-linux-gnu", "aarch64-unknown-linux-gnu"]
source_repository = "tailrocks/holla"
consumer_repository = "tailrocks/homebrew-holla"
description = "Adaptive dev environment CLI"

[[unit]]
id = "rust-holla-cli"
label = "Rust crate (holla-cli)"
kind = "rust"
root = "."
watch = ["**/*.rs", ".cargo/**", "Cargo.lock", "Cargo.toml", "benches/**", "build.rs", "examples/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "src/**", "tests/**"]
github_pr_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx test --locked --all-features --package 'holla-cli'", "mbx clippy --locked --profile test --all-targets --all-features --package 'holla-cli' -- -D warnings"]
github_full_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx test --locked --all-features --package 'holla-cli'", "mbx clippy --locked --profile test --all-targets --all-features --package 'holla-cli' -- -D warnings"]
velnor_pr_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx test --locked --all-features --package 'holla-cli'", "mbx clippy --locked --profile test --all-targets --all-features --package 'holla-cli' -- -D warnings"]
velnor_full_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx test --locked --all-features --package 'holla-cli'", "mbx clippy --locked --profile test --all-targets --all-features --package 'holla-cli' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
